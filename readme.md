my-project/                # 项目根目录（全小写，用短横线）
├── index.html             # 入口页面（必须在根目录）
├── assets/                 # 静态资源（统一放这，不乱丢）
│   ├── css/                # 所有样式文件
│   │   └── main.css        # 主样式（全局通用）
│   ├── js/                 # 所有脚本文件
│   │   └── main.js         # 主脚本（全局逻辑）
│   └── images/             # 图片资源（头像、背景等）
│       └── avatar.png
└── README.md               # 项目说明（写用途、技术、启动方式，好习惯）